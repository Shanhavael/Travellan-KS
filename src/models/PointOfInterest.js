class PointOfInterest {
  constructor(key, id, lat, lon, title, category) {
    this.key = key;
    this.id = id;
    this.lat = lat;
    this.lon = lon;
    this.title = title;
    this.category = category;
  }
}

export default PointOfInterest;
